/* This is a sample correct (lexically) input Porygon File */

int main(void) {
    /* Do some computations */
    int indx = 0;
    table sample =

    int arr[] = {1, 2, 3, 4};
    for (int num in arr) {
        write(num);
    }

    bool values[] = {false, false, false, true, false, true, false, false, false};
    values[3:7:2];

    table float balance[];
    balance = | ("id", "profit", "debt") : (0, 1, 2); (0.2, 0.9, 108.3); (2.3, 5.3, 9.6)|

    string valid = "A valid string can have multiple chars";

    return 0;
}
